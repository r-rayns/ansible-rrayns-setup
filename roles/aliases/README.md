# aliases

Adds custom shell aliases to `.zshrc`.

## Aliases

- `spck` - Spell check a word using aspell

**Usage**: `spck word` outputs spell check results for the given word.

## Usage

```bash
ansible-playbook setup.yml --tags aliases
```
